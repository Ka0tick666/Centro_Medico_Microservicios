server:
  port: 8088

spring:
  application:
    name: ms-seguro
  
  datasource:
    url: jdbc:mysql://localhost:3306/db_seguro?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root  # <-- Tu clave de MySQL local
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop # Borra y crea tablas limpias al arrancar
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: false
    status-page-url-path: /  # Manda el link de Eureka directo a tu JSON operativo
    instance-id: ${spring.application.name}:${server.port}