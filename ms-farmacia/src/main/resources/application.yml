server:
  port: 8085

spring:
  application:
    name: ms-farmacia
  
  datasource:
    url: jdbc:mysql://localhost:3306/db_farmacia?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root  # <-- Tu clave de MySQL aquí
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop # Limpia y crea las tablas frescas al arrancar
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
    status-page-url-path: /