server:
  port: 8086 # 👈 Puerto exclusivo para Laboratorio

spring:
  application:
    name: ms-laboratorio
  datasource:
    # Conexión centralizada al MySQL de Aiven con SSL
    url: jdbc:mysql://${DB_HOST:mysql-26a1585c-centro-medico.g.aivencloud.com:12885}/${DB_NAME:defaultdb}?useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true
    username: ${DB_USER:avnadmin}
    password: ${DB_PASSWORD:AVNS_FOJHhGkvfWqMm2wauK8}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop # 👈 Limpia y ejecuta el DataFaker de exámenes al arrancar
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# 👇 SECCIÓN EUREKA 👇
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
    status-page-url-path: /