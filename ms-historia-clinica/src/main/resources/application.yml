server:
  port: 8084

spring:
  application:
    name: ms-historia-clinica
  
  datasource:
    url: jdbc:mysql://localhost:3306/db_historial?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root  # <-- Tu clave de MySQL aquí
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop # Limpia las tablas al arrancar para que pruebes fresquito
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost
    status-page-url-path: /